# OS metadata
.DS_Store
Thumbs.db
desktop.ini
ehthumbs.db

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~
*.bak

# Logs
*.log

# Local environment / secrets — NEVER commit
.env
.env.local
.env.*.local
secrets/
*.pem
*.key

# Build artifacts (este sitio es estático, no debería haber, pero por si acaso)
node_modules/
dist/
build/
.cache/
.parcel-cache/

# Python (por si alguien usa inject_adsense.py local)
__pycache__/
*.pyc
*.pyo

# Temp
tmp/
temp/

# Documentos personales / no relacionados con el sitio
*.docx
*.doc
*.odt
*.xlsx
*.xls
*.pdf


# Landing standalone Google Ads (experimento 2026-05, NO forma parte del sitio AdSense)
# Se conservan en local como referencia/entregable, pero no se despliegan:
# publicarlos en Cloudflare Pages crearia contenido casi duplicado de cara a la revision AdSense.
_legacy/
hogarconectado_landing_AUDIT.md

# Documentacion interna y tooling (no desplegar en Cloudflare Pages)
.internal/
ADSENSE-APPLICATION.md
CLAUDE.md
NEWSLETTER-SETUP.md
PROMPT-mejora-landing.md
STRATEGY.md
BIENVENIDA-email.html
worker/
